[project]
name = "chufistov_decorators_georgechuff"
version = "0.0.1"
authors = [
  { name="Georgii Chufistov", email="geochuff@gmail.com" },
]
description = "Package with unique decorators"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[build-system]
requires = ["uv_build >= 0.11.7, <0.12.0"]
build-backend = "uv_build"
